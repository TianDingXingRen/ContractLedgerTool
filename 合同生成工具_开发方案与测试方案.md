# 合同生成工具——开发方案与测试方案

> 编制日期：2026-06-23  
> 分析基线：当前工作区代码 + `合同生成工具_软件功能说明文档.md`（版本 `20260617.163446`）  
> 适用范围：Windows 10/11，本地 Flask 应用，源码运行与 PyInstaller/离线安装包部署

---

## 1. 分析结论

当前项目已经具备完整产品雏形，不适合按“从零开发”推进。模板、合同生成、台账、付款计划、备份、回收站、项目跟踪和 Excel 单据均已有代码与测试基础。

建议采用“先稳定、再补齐、后发布”的路线：

1. 先修复批量生成、PDF、运行时目录和测试隔离等发布阻断问题。
2. 再补齐数字字段、真实预览、必填校验、数据一致性等规格差距。
3. 最后完成安全加固、性能验证、打包升级和 Windows 实机验收。

当前代码不宜直接发布。主要原因：

- 批量生成后，台账引用的 DOCX 文件会被立即删除，合同详情无法再次下载原文件。
- Word COM PDF 转换存在变量引用错误，并可能连接、退出用户已有的 Word 实例。
- 打包模式下，`config.json` 和 Excel 单据默认值的运行时目录设计不统一，存在配置或用户数据重启后丢失的风险。
- 自动化测试不是全绿，应用初始化与临时目录隔离存在系统性问题。
- 当前 Git 工作区约有 30 个已跟踪文件被修改、51 个未跟踪项，正式开发前需要先建立可回退基线。

---

## 2. 当前系统概况

### 2.1 架构

| 层次 | 当前实现 | 评价 |
|---|---|---|
| Web 层 | Flask + Jinja2，路由按模块拆分 | 基本清晰，可继续沿用 |
| 前端 | 原生 JavaScript + Alpine.js + 本地 Tailwind/DaisyUI 资源 | 轻量，但静态脚本重构后测试未同步 |
| 文档处理 | python-docx | 核心替换、跨 Run 占位符、表格行克隆已有测试 |
| 公式计算 | Python AST 白名单 | 安全基础较好；表格跨行聚合尚未接入生成流程 |
| 数据存储 | SQLite WAL + 手工迁移 | 功能完整度较高，但约束和迁移测试需加强 |
| Excel | openpyxl | 台账、付款计划、业务单据均已实现 |
| PDF | Word COM + LibreOffice | LibreOffice 路径可用性较高，Word COM 当前存在阻断缺陷 |
| 部署 | 源码、单 EXE、离线安装器 | 脚本较多，需要统一发布入口与持久化目录 |

### 2.2 代码规模与模块

核心代码集中在：

- `app.py`：运行时初始化、中间件、应用工厂、资源播种。
- `routes/`：模板、合同、付款、系统设置、Excel 单据。
- `template_def.py`：模板定义、校验、复制、版本管理。
- `docx_builder.py`：DOCX 文本替换和表格克隆。
- `field_eval.py`：安全公式解析与计算顺序。
- `ledger_store/__init__.py`：合同、付款计划、历史、备份、迁移。
- `payment_extractor.py`：付款条款规则提取。
- `pdf_exporter.py`、`xlsx_exporter.py`、`excel_bill_service.py`：导出服务。

目前最大的维护风险不是文件数量，而是运行时路径和全局状态散落在多个模块中，测试、源码运行、PyInstaller 运行采用了不同的隐含路径规则。

---

## 3. 功能完成度与规格差距

| 模块 | 当前状态 | 主要差距 |
|---|---|---|
| 模板管理 | 基本完成 | 缺少独立 `number` 字段；必填规则未在服务端强制；版本文件秒级命名可能覆盖 |
| 单份合同生成 | 基本完成 | 前端预览未使用用户当前填写值；台账失败后仍返回文件但仅通过响应头提示 |
| 批量合同生成 | 有严重缺陷 | 入账后删除每份 DOCX；无批次级原子性；单份失败后的处理不完整 |
| DOCX 写入 | 基本完成 | 需补充合并单元格、复杂表格、页眉页脚等兼容性边界说明 |
| 公式引擎 | 基本完成 | 仅支持标量参数聚合，`resolve_table_aggregate()` 未接入标量计算流程 |
| 合同台账 | 基本完成 | 合同编号未做唯一约束；批量状态变更不写历史；删除/恢复历史不完整 |
| 付款计划 | 基本完成 | 多行保存非事务化；付款状态与已付金额可互相矛盾；已付可大于应付 |
| Excel 台账/付款导出 | 已实现 | 缺少 Excel 公式注入防护和大数据量验证 |
| Excel 业务单据 | 已实现首版 | 合同明细硬编码为 `table_3`；默认值目录在打包模式下可能不持久化 |
| PDF 导出 | 部分完成 | Word COM 实现存在确定性错误、超时模型不可靠、可能影响用户 Word |
| 备份/恢复 | 基本完成 | 需补充 WAL 场景、升级前后恢复和损坏备份实测 |
| 开机自启动 | 已实现 | 实现已改为 `start.ps1 -NoBrowser`，旧测试仍断言 `--no-browser` |
| 安全 | 基础较好 | DOCX 仅校验 ZIP 魔数；缺少 ZIP 解压炸弹检测；Excel 公式注入未处理；LAN 模式无认证 |
| 打包发布 | 有多套脚本 | 缺少唯一发布入口、干净环境构建、安装/升级/卸载自动验收 |

---

## 4. 优先级问题清单

### 4.1 P0：发布阻断

#### P0-1 批量合同台账文件失效

证据：

- `routes/contracts_bp.py:245` 将每份生成文件路径写入合同台账。
- `routes/contracts_bp.py:258` 随后删除同一个 DOCX 文件。

影响：

- 批量生成的合同能出现在台账中，但详情页“下载合同”会返回文件不存在。
- 台账记录与文件系统不一致，后续 PDF 导出也无法使用。

方案：

- 每份批量 DOCX 使用永久输出路径，ZIP 直接读取该文件，不再删除。
- 仅删除真正的临时副本；永久文件由台账引用并受清理任务保护。
- 单份生成失败时不得入账、不得写入 ZIP。
- 增加批次结果清单：成功数、失败数、合同 ID、文件名、错误原因。
- 明确批量策略：推荐“允许部分成功，但必须返回清单并保持每份合同原子性”。

#### P0-2 Word COM PDF 导出不可可靠使用

证据：

- `pdf_exporter.py:197`、`pdf_exporter.py:199` 使用了当前函数作用域中不存在的 `abs_docx`、`abs_pdf`。
- 实现尝试 `GetActiveObject`，随后无条件执行 `word.Quit()`。
- `ThreadPoolExecutor` 上下文退出会等待工作线程，COM 卡死时 30 秒超时未必能真正结束请求。

影响：

- 检测到 Word 时仍可能转换失败。
- 可能连接并退出用户已经打开的 Word。
- 卡死场景可能拖住 Flask 请求线程。

方案：

- 修正参数引用，统一使用 `docx_path`、`pdf_path` 的绝对路径。
- 只使用隔离的 Word 实例，不连接用户活动实例。
- 将 COM 转换放入独立子进程；超时后终止子进程，而不是等待 Python 线程。
- 保留 LibreOffice 兜底，并统一诊断逻辑与实际搜索路径。
- 增加 Word、LibreOffice、二者均无三种实机测试。

#### P0-3 应用工厂和测试运行时无法隔离

证据：

- `app.py` 在模块导入时直接执行 `app = create_app()`。
- `tests/conftest.py` 只重设了部分 `app` 路径，没有同步重设 `template_def`、`ledger_store` 等模块路径。
- 测试初始化时资源源路径与目标路径可能相同，触发文件复制异常。
- 测试结束时日志文件处理器仍占用临时目录中的 `app.log`。

影响：

- 7 个 Excel 单据接口集成测试无法初始化。
- 测试可能读写真实工作区数据，不满足隔离要求。
- 后续 CI 难以稳定复现。

方案：

- 引入统一的 `RuntimePaths`，集中保存 templates/uploads/output/sessions/data/logs/config 路径。
- `create_app()` 接受路径覆盖、测试配置和 `run_maintenance=False`。
- 测试中禁止自动备份、清理、资源播种和打开系统目录。
- 增加日志显式关闭方法，在 fixture teardown 中释放文件句柄。
- 保留生产入口的全局 `app`，但业务初始化必须可注入、可重复创建。

#### P0-4 打包版配置和用户默认值持久化不可靠

证据：

- `config.py` 使用自身 `__file__` 目录定位 `config.json`，PyInstaller 下通常是临时资源目录。
- `excel_bill_service.py` 同样使用自身 `__file__` 目录保存 `data/excel_bill_defaults`。

影响：

- 安装目录中的 `config.json` 可能未被读取。
- Excel 单据默认值可能在应用重启或版本升级后丢失。

方案：

- 所有可写数据统一放到 `RuntimePaths.base_dir` 下。
- 配置加载顺序固定为：默认值 < 外部 `config.json` < `CT_*` 环境变量 < CLI 参数。
- 打包资源目录只读，只存 HTML、静态资源和内置模板。
- 增加“保存默认值—重启—读取”和“升级安装—数据保留”测试。

### 4.2 P1：规格补齐与数据一致性

#### P1-1 字段模型补齐

- 增加 `number` 字段类型：HTML 数字输入、最小值、最大值、小数位、服务端解析。
- 服务端强制校验必填字段，不能只依赖前端确认框。
- 对 select 选项做服务端白名单校验。
- 表格列同步支持 number/select/textarea/calculated。

#### P1-2 实现真实合同预览

当前预览接口使用字段标签或默认值，不读取编辑器当前表单。

改造后应复用正式生成链路：

1. 解析当前表单。
2. 校验并计算表格字段。
3. 计算标量公式。
4. 生成临时 DOCX。
5. 不写台账、不提取付款计划。
6. 临时文件按短 TTL 清理。

预览和正式生成必须使用同一个服务，避免结果不一致。

#### P1-3 批量生成一致性

- 每份合同采用“生成文件 + 创建合同 + 插入付款计划”的单份原子流程。
- 文件写入失败不得入账。
- 数据库写入失败应删除对应新文件。
- ZIP 创建失败不应破坏已成功合同，但必须生成可追溯的错误清单。
- 重复对方单位名称需要自动生成不冲突的 ZIP 文件名。

#### P1-4 合同数据约束与历史

- 对非空合同编号增加唯一索引；迁移前先检查并处理历史重复数据。
- 批量状态变更写入 `contract_history`。
- 软删除、恢复、永久删除增加操作历史或审计日志。
- 更新合同前统一执行长度、日期、金额和状态流转校验。

#### P1-5 付款计划一致性

- 一次保存多行计划使用单事务。
- 强制 `0 <= paid_amount <= due_amount`；应付为空时明确允许规则。
- 根据金额自动推导付款状态，或校验用户选择与金额一致。
- 已付清时要求已付日期；未付款时清空已付日期。
- 比例超出 0～100 应返回错误，不应静默截断。

#### P1-6 Excel 单据通用化

- 不再只识别 `table_3`，优先按模板字段元数据选择“采购明细表”。
- 保留 `table_3` 作为旧模板兼容规则。
- 限制单据明细行数、字段长度和请求 JSON 大小。
- 对以 `= + - @` 开头的文本做 Excel 公式注入防护。

#### P1-7 DOCX 上传加固

- 除 ZIP 魔数外，校验 DOCX 必需项，如 `[Content_Types].xml`、`word/document.xml`。
- 限制 ZIP 文件条目数、单条目解压大小和总解压大小。
- 拒绝加密、损坏或异常压缩比文件。
- 解析失败时清理上传文件和会话数据。

### 4.3 P2：工程化和体验优化

- 将合同生成编排从路由提取为 `GenerationService`。
- 将 SQLite 操作按合同、付款、备份拆分仓储模块，保留统一事务入口。
- 统一错误对象和 JSON/HTML 响应。
- 合并重复的构建脚本，形成一个参数化发布命令。
- 增加 CI：Python 3.10/3.12、单元测试、静态检查、前端脚本语法检查。
- 增加数据库和文件系统容量告警、备份最近成功时间。
- 对首页统计和大列表建立性能基线。

---

## 5. 建议开发架构

### 5.1 运行时目录

建议新增统一对象：

```text
RuntimePaths
├── base_dir
├── resource_dir
├── config_file
├── templates_dir
├── uploads_dir
├── output_dir
├── sessions_dir
├── data_dir
├── database_file
├── backups_dir
└── logs_dir
```

原则：

- `resource_dir` 只读，来自源码目录或 PyInstaller `_MEIPASS`。
- 其他目录均为持久化可写目录。
- 测试可一次性将所有路径替换为同一个临时根目录。

### 5.2 生成服务

建议增加统一生成服务，供预览、单份、批量共同调用：

```text
解析输入
  → 服务端校验
  → 表格公式计算
  → 标量公式计算
  → DOCX 生成
  → 可选付款提取
  → 可选台账事务
  → 返回生成结果
```

返回对象至少包含：

- `success`
- `output_path`
- `contract_id`
- `payment_plan_count`
- `warnings`
- `errors`

### 5.3 数据库迁移

建议新增迁移：

- 合同编号非空唯一索引。
- 必要的项目、删除状态、付款查询组合索引复核。
- 如果增加批次追踪，可新增 `generation_batch_id`，但不作为首轮必需项。

迁移要求：

- 从无数据库直接初始化。
- 从 v1/v2 等历史数据库逐级升级。
- 重复执行幂等。
- 迁移失败不改变原数据库。
- 升级前自动备份。

---

## 6. 分阶段开发计划

以下按人日估算，适合一名全栈工程师执行；多人并行时以依赖关系为准。

| 阶段 | 工作内容 | 预计人日 | 交付标准 |
|---|---|---:|---|
| M0 基线固化 | 保存当前工作区、建立功能清单、固定测试数据、记录已知失败 | 1～2 | 可回退、测试结果可复现 |
| M1 稳定性修复 | P0-1～P0-4：批量文件、PDF、应用工厂、运行时目录 | 5～7 | 发布阻断项全部关闭，集成测试可隔离运行 |
| M2 核心规格补齐 | number、必填、真实预览、批量一致性、表格聚合 | 6～9 | 模板到合同生成主链路与目标文档一致 |
| M3 数据与安全 | 合同唯一性、历史、付款事务、DOCX/Excel 安全 | 5～7 | 数据约束明确，无高风险输入漏洞 |
| M4 打包与兼容 | 构建脚本统一、配置持久化、Word/LO、自启动、升级 | 4～6 | 干净 Windows 环境安装、重启、升级均通过 |
| M5 全量测试与验收 | 回归、E2E、性能、安全、UAT、文档更新 | 5～7 | 达到发布退出标准 |

总量：约 26～38 人日。

建议排期：

- 单人：5～8 周。
- 两人并行：3～5 周，其中 PDF/打包测试必须在 Windows 实机串行验收。

---

## 7. 测试现状

### 7.1 2026-06-23 测试结果

执行：

```powershell
python -m pytest -q
```

结果：

- 161 通过
- 5 跳过
- 4 失败
- 8 错误

### 7.2 异常分类

| 异常 | 分类 | 处理建议 |
|---|---|---|
| `test_editor_script_has_valid_syntax` | 执行环境问题 | Node 因目录权限无法 `lstat`；已通过 `vm.Script` 验证脚本语法有效 |
| 编辑器页面断言 `showGenerationResult` 必须内联 | 测试失配 | 函数已迁移到 `static/js/editor.js`，测试应检查静态资源与页面引用 |
| 两个自启动测试要求 `--no-browser` | 测试失配 | 当前使用 `start.ps1 -NoBrowser`，功能语义等价，应更新契约 |
| 7 个 Excel 单据接口测试初始化错误 | 测试架构缺陷 | 应用路径未完全隔离，资源复制源/目标冲突，日志文件未释放 |
| 项目 Excel 导出测试 `tmp_path` 错误 | 当前受限环境问题 | pytest 临时目录无权限，CI 应指定可写 `--basetemp` |

结论：现有单元测试覆盖较多，但“测试全绿”尚不能作为当前代码的发布证据。

---

## 8. 测试总体策略

### 8.1 测试分层

| 层级 | 目标 | 工具 |
|---|---|---|
| 单元测试 | 公式、字段解析、路径安全、付款提取、数据校验 | pytest |
| 服务测试 | 生成服务、批量策略、付款保存事务 | pytest + 临时目录/SQLite |
| 接口集成 | Flask 路由、中间件、CSRF、错误码 | Flask test client |
| 文件产物 | DOCX、XLSX、ZIP、PDF 内容与格式 | python-docx、openpyxl、PDF 文本/页数检查 |
| 浏览器 E2E | 上传、配置、填写、生成、下载、台账操作 | Playwright，Edge/Chrome |
| 安装包测试 | 安装、启动、自启动、升级、卸载、数据保留 | Windows 10/11 虚拟机 |
| 非功能测试 | 性能、容量、并发、安全、恢复 | pytest benchmark/定制脚本 |

### 8.2 测试环境矩阵

| 环境 | 必测项 |
|---|---|
| Python 3.10 + Windows 10 | 源码运行、核心回归 |
| Python 3.12 + Windows 11 | 源码运行、核心回归、构建 |
| Edge、Chrome | 主要 E2E |
| Word 2013/2016/2019/365 至少两组 | Word COM PDF |
| 无 Word + LibreOffice | PDF 兜底 |
| 无 Word、无 LibreOffice | 友好错误与诊断 |
| 单 EXE/离线安装版 | 配置、默认值、数据库、模板持久化 |

---

## 9. 核心测试用例矩阵

| 编号 | 场景 | 验证点 |
|---|---|---|
| TPL-01 | 上传合法 DOCX | 成功检测段落和表格占位符 |
| TPL-02 | 非 DOCX、损坏 ZIP、压缩炸弹 | 拒绝上传且无残留文件 |
| TPL-03 | 200 字段、50 列边界 | 边界成功，超限返回 400 |
| TPL-04 | 版本保存与快速连续保存 | 不覆盖历史版本，可正确回滚 |
| TPL-05 | number/select/required | 前后端校验一致 |
| GEN-01 | 单份生成 | 值、格式、表格、公式均正确；自动入账 |
| GEN-02 | 当前值预览 | 预览与正式生成内容一致，且不入账 |
| GEN-03 | 100 份批量生成 | ZIP 100 份、台账 100 条、每条原文件可下载 |
| GEN-04 | 批量部分失败 | 成功项可用，失败项不入账，有完整错误清单 |
| GEN-05 | 重复对方单位 | ZIP 内无同名覆盖，合同编号不冲突 |
| DOC-01 | 占位符跨多个 Run | 替换正确且保留周边样式 |
| DOC-02 | 表格克隆、空表、序号列 | 行数、格式、编号正确 |
| DOC-03 | 合并单元格/复杂表格 | 不崩溃；不支持时返回明确错误 |
| CAL-01 | 四则运算和依赖排序 | 正确计算并检测循环依赖 |
| CAL-02 | 安全表达式攻击 | 属性、调用、幂、超长 AST 全部拒绝 |
| CAL-03 | 表格行计算和跨行聚合 | 前端显示与服务端最终值一致 |
| LED-01 | 合同编号重复 | 第二次写入被明确拒绝 |
| LED-02 | 单条/批量状态变化 | 均写入历史，旧值新值正确 |
| LED-03 | 回收站 | 软删除、恢复、永久删除及关联数据正确 |
| PAY-01 | 典型付款条款 | 比例、金额、事件、日期、置信度正确 |
| PAY-02 | 税率/违约金等干扰文本 | 不生成错误付款计划 |
| PAY-03 | 300 行保存边界 | 单事务提交，任一无效则全部回滚 |
| PAY-04 | 已付金额和状态 | 状态、金额、日期保持一致 |
| PAY-05 | 即将到期/下月跨年 | 日期边界和未付金额正确 |
| XLS-01 | 台账/付款导出 | 表头、数据、合计、项目字段正确 |
| XLS-02 | 公式注入文本 | 打开 Excel 时不执行用户输入公式 |
| BILL-01 | 合同表格映射单据 | 映射、默认值、行号、金额正确 |
| BILL-02 | 默认值重启持久化 | 源码版和安装版均保留 |
| PDF-01 | Word 转换 | 内容、页数正确，不关闭用户 Word |
| PDF-02 | LibreOffice 兜底 | Word 失败时自动成功转换 |
| PDF-03 | 转换卡死 | 在规定时间内结束并释放进程 |
| SYS-01 | 备份/恢复 | 数据完整、恢复前自动备份 |
| SYS-02 | 损坏备份 | 恢复前拒绝，不破坏当前数据库 |
| SYS-03 | 自动清理 | 仅删除过期临时文件，受引用文件不删 |
| SYS-04 | 自启动 | 开启、重启生效、关闭后不再启动 |
| PKG-01 | 全新安装 | 无 Python 环境可正常运行 |
| PKG-02 | 覆盖升级 | 数据库、用户模板、配置、默认值不丢失 |
| SEC-01 | CSRF/路径穿越/限流 | 非法请求被拒绝，合法请求不误伤 |
| SEC-02 | LAN 绑定 | 若允许非回环地址，必须启用认证或明确禁止 |

---

## 10. 性能与容量测试

目标沿用功能说明文档，并增加可测量口径：

| 指标 | 目标 | 测量条件 |
|---|---:|---|
| 应用冷启动 | < 3 秒 | 已安装环境，不含浏览器启动 |
| 单份 DOCX 生成 | < 3 秒 | 20 标量字段、1 个 50 行表格 |
| 批量 100 份 | < 60 秒 | 不生成 PDF，含台账和付款提取 |
| 付款条款提取 | < 5 秒 | 100 页以内 DOCX |
| 台账列表 | < 1 秒 | 1 万合同、分页 20 条 |
| 付款列表 | < 1 秒 | 5 万付款计划、组合筛选 |
| Excel 导出 | < 10 秒 | 1 万台账或付款记录 |
| PDF 导出 | < 30 秒 | Word 正常可用；超时必须可终止 |

需要记录 P50、P95 和最大内存，不只判断一次执行是否成功。

---

## 11. 发布验收标准

发布前必须同时满足：

1. P0、P1 问题全部关闭，或有书面接受的延期项。
2. 自动化测试全绿；跳过项必须有明确环境原因和独立实机结果。
3. 批量生成 100 份后，每条台账原文件均可下载。
4. Word 与 LibreOffice PDF 路径至少各完成一次实机验收。
5. Windows 10、Windows 11 安装包冒烟测试通过。
6. 覆盖升级后数据库、用户模板、配置、Excel 默认值全部保留。
7. 备份恢复演练通过，损坏备份不会覆盖当前数据库。
8. 性能指标达到目标，或已形成基线与批准的优化计划。
9. `README.md`、软件规格说明、功能说明和版本号与代码一致。
10. Git 工作区干净，发布版本可由固定命令重新构建。

---

## 12. 建议首轮迭代任务

首轮迭代应严格控制在“形成可发布基线”：

1. 固化当前代码快照和测试数据。
2. 重构运行时路径和测试应用工厂。
3. 修复批量合同文件生命周期。
4. 重写 Word COM PDF 隔离与超时机制。
5. 修复打包版配置和 Excel 默认值持久化。
6. 更新失配测试，使测试结果能真实反映产品状态。
7. 增加四个发布门禁测试：
   - 批量入账后下载原文件。
   - 预览不入账且与正式生成一致。
   - 安装版重启后配置/默认值存在。
   - PDF 转换不影响已有 Word 进程。

完成以上任务后，再开始字段模型和业务体验增强，能显著降低返工风险。

---

## 13. 本轮实施结果（2026-06-23）

### 13.1 实施范围

按后续确认，本轮不修改 PDF 导出与安全功能。`pdf_exporter.py`、DOCX 上传加固、Excel 公式注入防护和 LAN 认证不在本轮实现范围；工作区中原有的相关改动保持不动。

### 13.2 已完成

| 项目 | 实施结果 |
|---|---|
| 统一运行时目录 | 新增 `RuntimePaths`，统一配置、模板、上传、输出、会话、数据库、备份、日志和 Excel 单据默认值目录 |
| 可测试应用工厂 | `create_app()` 支持运行时目录、资源目录、测试模式和维护任务开关；测试结束显式释放日志句柄 |
| 打包数据持久化 | 可写数据与只读资源目录分离，配置和 Excel 单据默认值不再写入 PyInstaller 临时资源目录 |
| 批量合同生成 | 永久保留台账引用的 DOCX；单份失败清理文件且不入账；ZIP 名称去重；批量合同编号自动增加顺序后缀 |
| 当前值预览 | 预览与正式生成复用输入解析、服务端校验和公式计算，不写台账、不生成付款计划 |
| 字段模型 | 增加数字字段、最小值/最大值/小数位；服务端强制必填和下拉白名单；表格列支持数字和下拉 |
| 公式引擎 | 前后端均支持 `SUM(table, column)` 等表格跨行聚合，并纳入依赖计算 |
| 模板版本 | 版本文件名增加微秒，兼容旧格式，避免连续保存覆盖 |
| 合同一致性 | 增加非空合同编号唯一索引及历史重复数据迁移；批量状态、软删除和恢复写入历史 |
| 付款一致性 | 多行保存使用单事务；校验已付金额范围；自动推导未付/部分/已付状态；已付款要求付款日期；比例超限返回错误 |
| Excel 单据 | 根据模板元数据或合同值自动发现明细表，保留 `table_3` 旧模板兼容逻辑 |
| 构建与文档 | 补齐打包隐藏依赖，README 同步数字字段、预览、运行时目录与测试方式 |

### 13.3 自动化测试结果

执行：

```powershell
python -m pytest -q -p no:cacheprovider --tb=short --basetemp .pytest_basetemp_release
```

结果：

- 181 通过
- 5 跳过（均为现有环境条件项）
- 0 失败
- 0 错误

新增或加强的验收覆盖：数字/必填/下拉校验、数字字段页面渲染与内联 JavaScript 语法、表格聚合、合同编号唯一性和历史、付款事务回滚、通用 Excel 明细识别、当前值预览、批量原文件下载、运行时默认值持久化。

### 13.4 后续实机验收

以下项目需要在目标 Windows/浏览器环境单独执行，不作为本轮自动化通过项：

- Windows 10/11 安装、启动、覆盖升级、卸载和数据保留。
- Edge/Chrome 完整交互 E2E。本轮本地浏览器自动化被运行环境策略拒绝，已用 Flask 页面渲染、路由集成和 JavaScript 语法测试覆盖可自动验证部分。
- PDF/Word/LibreOffice 验收（按确认不修改 PDF，保留原实现）。
- 100 份批量、1 万合同列表和大数据 Excel 导出的正式性能基线。
- P2 的仓储模块拆分、统一错误对象和完整 CI 流水线属于后续结构性优化；本轮仅先提取并复用生成输入准备流程，避免在功能修复期扩大重构风险。
