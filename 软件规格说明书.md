# 合同生成工具 — 软件规格说明书

> **版本**: 1.0
> **文档状态**: 正式版
> **适用范围**: 内部使用
> **最后更新**: 2026-06-01

---

## 目录

1. [概述](#1-概述)
2. [系统架构](#2-系统架构)
3. [功能模块规格](#3-功能模块规格)
4. [数据模型](#4-数据模型)
5. [接口规范](#5-接口规范)
6. [安全设计](#6-安全设计)
7. [部署与运行](#7-部署与运行)
8. [非功能需求](#8-非功能需求)
9. [附录](#9-附录)

---

## 1. 概述

### 1.1 产品简介

**合同生成工具** 是一款基于模板的合同文档批量生成与管理系统，采用 B/S 架构（Flask Web 后端 + 浏览器前端）。系统支持 DOCX 模板制作、字段可视化配置、单一/批量合同生成、台账管理、付款计划自动提取与跟踪，以及 Excel 导出。

### 1.2 目标用户

- 企业法务/行政人员：需要快速生成标准合同的办公场景
- 采购/销售人员：需要批量生成采购/销售合同
- 财务人员：需要跟踪合同付款计划

### 1.3 核心能力矩阵

| 能力 | 说明 |
|------|------|
| 模板制作 | 上传 DOCX 参考文件，自动检测 `{字段名}` 占位符，可视化配置字段类型 |
| 合同生成 | 逐个或批量（最多100份/次）生成合同，自动写入台账 |
| 台账管理 | 搜索、筛选、状态更新、批量操作、变更历史追踪 |
| 付款计划 | 自动从合同文本提取付款条款，支持手动确认和编辑 |
| 文档导出 | PDF（Word COM / LibreOffice）和 Excel（openpyxl） |
| 版本管理 | 模板修改自动备份历史版本，支持回滚 |
| 数据库管理 | SQLite 数据库自动备份、完整性校验、恢复 |
| 自启动 | Windows 系统支持开机自启动（可选） |

### 1.4 技术栈

| 层级 | 技术选型 |
|------|----------|
| 语言 | Python 3.10+ |
| Web 框架 | Flask 3.x |
| 文档处理 | python-docx（DOCX读写）、pywin32（Word COM PDF导出） |
| 电子表格 | openpyxl（Excel导出） |
| 数据库 | SQLite 3（WAL模式） |
| 模板引擎 | Jinja2（服务端渲染） |
| 公式引擎 | AST 白名单安全求值（自研） |
| 部署 | 内嵌 Flask 开发服务器 / 打包为 Windows 可执行文件 |

---

## 2. 系统架构

### 2.1 整体架构

```
浏览器前端 (Jinja2 SSR + 内嵌 CSS/JS)
        | HTTP/HTML
        v
+-----------------------------------------+
|          Flask Web 主程序                 |
|  +----------+ +---------+ +----------+  |
|  | 路由层    | |CSRF检测 | |限流器    |  |
|  | 4个蓝图   | |(hmac)  | |(双层LRU) |  |
|  +-----+----+ +---------+ +----------+  |
|        |                                 |
|  +-----+-----------------------------+   |
|  |        业务逻辑层                  |   |
|  |  template_def  docx_builder       |   |
|  |  field_eval    payment_extract    |   |
|  |  docx_builder   xlsx_exporter      |   |
|  |  ledger_store                     |   |
|  +-----+-----------------------------+   |
|        |                                 |
|  +-----+-----------------------------+   |
|  |           数据层                   |   |
|  |  SQLite (WAL) + 文件系统          |   |
|  |  templates/ uploads/ output/      |   |
|  |  sessions/ data/ logs/            |   |
|  +----------------------------------+   |
+-----------------------------------------+
```

### 2.2 模块划分

| 模块 | 文件 | 职责 |
|------|------|------|
| **主程序** | `app.py` | Flask 应用工厂、目录初始化、CSRF/限流中间件、路由注册、信号处理 |
| **配置管理** | `config.py` | config.json 读取 + 环境变量 CT_* 覆盖 |
| **模板定义** | `template_def.py` | .contract-template JSON 文件的 CRUD、版本备份、回滚 |
| **DOCX 写入** | `docx_builder.py` | 段落文本替换、表格行克隆、单元格精确定位写入 |
| **公式求值** | `field_eval.py` | AST 白名单安全求值、计算字段依赖拓扑排序 |
| **台账存储** | `ledger_store.py` | SQLite 数据库操作、合同/付款计划 CRUD、备份/恢复 |
| **付款提取** | `payment_extractor.py` | 规则引擎从合同文本自动提取付款条款 |
| **Excel 导出** | `xlsx_exporter.py` | openpyxl 生成付款计划表和合同台账表 |
| **工具库** | `utils/` | 安全校验、字段解析、生成工具、会话管理、日志、常量枚举 |
| **路由蓝图** | `routes/` | 4个蓝图：模板、合同、付款、设置 |

### 2.3 数据流

```
上传DOCX -> 检测{字段名} -> 配置字段 -> 保存模板JSON
                                      |
选择模板 -> 填写字段 -> 公式求值 -> DOCX生成 -> 台账入库
                                    |
                            +-------+-------+
                            v               v
                    Word/LO->PDF      付款提取->付款计划
```

---

## 3. 功能模块规格

### 3.1 模板管理模块

#### 3.1.1 模板创建流程

上传 DOCX -> 解析占位符 {xxx} -> 配置字段 -> 保存 .contract-template

**上传规格**:
- 接受格式：仅 `.docx`
- 文件大小限制：50MB（可配置 `MAX_CONTENT_LENGTH_MB`）
- 文件校验：检查 ZIP 文件头魔数 `PK`，拒绝非 Office 文档

**占位符检测** (`utils/field_utils.detect_markers`):
- 正则模式：`\{\{?(.+?)\}\}?` — 同时支持单花括号 `{字段名}` 和双花括号 `{{字段名}}` 格式
- 自动去重、按文档顺序排列
- 返回字段列表（label、placeholder、location 定位信息）
- 位置类型：
  - `paragraph` — 段落中的占位符，记录 body_index 和占位符文本
  - `table_cell` — 表格单元格中的占位符，记录 table_index/row_index/col_index

#### 3.1.2 字段类型定义

| 字段类型 | 标识 | 说明 | 必填配置 |
|----------|------|------|----------|
| **文本** | `text` | 单行文本输入 | — |
| **长文本** | `textarea` | 多行文本输入 | — |
| **下拉选择** | `select` | 预设选项下拉 | `options`（选项列表，最多100项） |
| **表格** | `table` | 多列重复数据行 | `columns`（列定义）、`template_row_index` |
| **计算** | `calculated` | 基于其他字段的公式计算 | `formula`（AST安全表达式） |

**表格列子类型**：列字段同样支持 `text` / `textarea` / `select` / `calculated`

**字段数量限制**:
- 模板字段数：<= 200 (`MAX_TEMPLATE_FIELDS`)
- 表格列数：<= 50 (`MAX_TABLE_COLUMNS`)
- 表格行数：<= 500 (`MAX_TABLE_ROWS`)

#### 3.1.3 模板存储格式 (.contract-template)

```json
{
  "format_version": "1.0",
  "template_name": "订货合同模板",
  "source_docx": "uuid.docx",
  "fields": [
    {
      "id": 0,
      "key": "seller_name",
      "label": "卖方名称",
      "field_type": "text",
      "required": true,
      "default_value": "",
      "location": {
        "type": "paragraph",
        "body_index": 2,
        "placeholder": "{卖方名称}"
      }
    },
    {
      "id": 5,
      "key": "order_items",
      "label": "订货清单",
      "field_type": "table",
      "columns": [
        {"key": "product_name", "label": "产品名称", "field_type": "text"},
        {"key": "qty", "label": "数量", "field_type": "text"},
        {"key": "unit_price", "label": "单价", "field_type": "text"},
        {"key": "subtotal", "label": "小计", "field_type": "calculated", "formula": "qty * unit_price"}
      ],
      "location": {
        "type": "table",
        "table_index": 1,
        "template_row_index": 1
      }
    }
  ]
}
```

#### 3.1.4 版本管理

- **自动备份**：每次保存模板时自动将旧版本备份到 `templates/versions/<模板名>/YYYYMMDD_HHmmss.contract-template`
- **回滚操作**：从版本列表中选择恢复，当前版本也会被备份（带 `_before_restore` 后缀）
- **预览**：支持预览模板填充效果（生成占位文档）

### 3.2 合同生成模块

#### 3.2.1 生成流程

选择模板 -> 填写字段值 -> 计算字段求值 -> 写入 DOCX -> 台账入库

**执行步骤**:
1. **字段值解析** (`parse_submitted_field_values`): 从表单提取各字段值，表格字段解析为 [{col: value}] 格式
2. **表格列定义应用** (`apply_submitted_table_columns`): 允许前端在生成时覆写表格列定义
3. **表格公式计算** (`recalculate_table_fields`): 计算表格内部的计算列（如 `小计 = 单价 x 数量`）
4. **标量公式计算** (`recalculate_scalar_fields`): 计算模板级别的计算字段（支持 SUM()/AVG() 聚合表格列）
5. **DOCX 写入** (`generate_docx_document`):
   - 有源文档：打开源 DOCX -> 按写入顺序逐个字段定位替换 -> 保存
   - 无源文档：`generate_from_scratch` 从模板定义直接生成新文档
6. **台账入账** (`create_ledger_record`): 自动推断合同摘要（标题/对方单位/金额/日期等）

#### 3.2.2 字段写入顺序

1. 表格字段优先写入（表头 + 数据行），确保表格定位不受后续段落写入影响
2. 输入/文本字段按依赖拓扑顺序写入
3. 计算字段最后写入（此时所有依赖字段的值已确定）

#### 3.2.3 批量生成

填写共享字段值 -> 输入对方单位列表 -> 指定对方单位字段映射 -> 逐份生成并打包为 ZIP

- 限制：<= 100 份/次 (`MAX_BATCH_CONTRACTS`)
- 对方单位名称：<= 120 字符 (`MAX_COUNTERPARTY_LENGTH`)
- 每份合同独立入台账
- 输出：ZIP 压缩包
- 中间文件即时清理

### 3.3 公式求值引擎

#### 3.3.1 安全设计

基于 Python `ast.parse`，仅允许白名单操作：

| 允许的操作 | 说明 |
|-----------|------|
| 四则运算 | `+` `-` `*` `/` |
| 一元运算 | 正号 `+x`、负号 `-x` |
| 聚合函数 | `SUM()` `AVG()` `MAX()` `MIN()` `COUNT()` |
| 数字字面量 | 整数和浮点数常量 |
| 变量引用 | AST `Name` 节点（从上下文字典取值） |

**安全限制**:

| 限制项 | 值 |
|--------|-----|
| 公式最大长度 | 500 字符 |
| AST 最大节点数 | 100 个 |
| 函数最大参数数 | 100 个 |
| 列表最大元素数 | 100 个 |
| 数值最大绝对值 | 10^12（1万亿） |

**禁止的操作**: 函数调用（除白名单外）、属性访问、比较运算、逻辑运算、字符串操作、列表推导、字典、集合、代码注入

#### 3.3.2 依赖拓扑排序

`sort_fields_by_dependency`:
- 自动分析计算字段的公式依赖（通过 AST 遍历提取变量名）
- 拓扑排序确保计算字段在依赖字段之后求值
- 检测循环依赖并报错
- 支持显式依赖声明 `depends_on`（用于表格聚合等AST无法自动检测的场景）

#### 3.3.3 表格聚合

`resolve_table_aggregate`:
- 从表格数据中提取指定列的值，进行聚合运算
- 支持：SUM、AVG、MAX、MIN、COUNT
- 示例：SUM(order_items, amount) — 对 order_items 表格的 amount 列求和

### 3.4 台账管理模块

#### 3.4.1 contracts 表结构

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增主键 |
| contract_no | TEXT UNIQUE | 合同编号（自动生成或手动指定） |
| title | TEXT NOT NULL | 合同标题 |
| counterparty | TEXT | 对方单位 |
| amount | REAL | 合同金额 |
| sign_date | TEXT | 签订日期 (YYYY-MM-DD) |
| owner | TEXT | 负责人 |
| status | TEXT NOT NULL | 状态（draft/signed/active/completed/void） |
| template_name | TEXT | 使用的模板名称 |
| docx_path | TEXT | DOCX 文件路径 |
| values_json | TEXT | 字段值快照 (JSON) |
| created_at | TEXT | 创建时间 |
| updated_at | TEXT | 更新时间 |

#### 3.4.2 合同状态流转

```
draft -> signed -> active -> completed
  |                |
  +----------------+---> void（任意状态可作废）
```

| 状态值 | 中文标签 |
|--------|----------|
| `draft` | 草稿 |
| `signed` | 已签订 |
| `active` | 履行中 |
| `completed` | 已完成 |
| `void` | 已作废 |

#### 3.4.3 台账功能

- **列表查看**: 分页展示（20条/页），显示合同编号、标题、对方单位、金额、状态、付款计划统计
- **关键词搜索**: 支持搜索合同编号、标题、对方单位、负责人、字段值JSON、付款计划文本
- **状态筛选**: 按合同状态过滤
- **详情查看**: 完整合同信息 + 付款计划列表 + 变更历史
- **合同编辑**: 修改各字段（记录变更历史到 contract_history 表）
- **数据下载**: 重新下载 DOCX
- **批量操作**: 批量删除（含关联数据）、批量变更状态

#### 3.4.4 合同台账 Excel 导出

- 列：序号、合同编号、合同名称、对方单位、金额、签订日期、负责人、状态、创建时间、付款计划数
- 样式：标题行加粗居中、表头蓝底、数据行细边线、合计行金额汇总

### 3.5 付款计划模块

#### 3.5.1 payment_plans 表结构

| id | INTEGER PK | 自增主键 |
| contract_id | INTEGER FK | 所属合同 |
| phase_name | TEXT | 款项名称 |
| payment_type | TEXT | 类型（conditional/fixed_date） |
| trigger_event | TEXT | 触发事件 |
| trigger_days | INTEGER | 触发后天数 |
| expected_trigger_date | TEXT | 预计触发日期 |
| due_date | TEXT | 应付日期 |
| ratio | REAL | 付款比例 (%) |
| due_amount | REAL | 应付金额 |
| paid_amount | REAL | 已付金额 |
| paid_date | TEXT | 实付日期 |
| condition_text | TEXT | 付款条件描述 |
| source_text | TEXT | 原文摘录 |
| confidence | TEXT | 置信度（high/medium/low） |
| confirm_status | TEXT | 确认状态（pending/confirmed/void） |
| payment_status | TEXT | 付款状态（unpaid/partial/paid） |
| remark | TEXT | 备注 |

#### 3.5.2 自动提取引擎 (`payment_extractor`)

**提取流程**:
1. 提取 DOCX 全文（段落 + 表格）
2. 按句号/分号/换行分割为片段
3. 识别包含付款关键词的候选片段（排除赔偿、违约金等干扰项）
4. 提取：比例（30%、百分之三十）、金额（人民币 50,000元）、日期（YYYY-MM-DD、X月X日）、天数（N 个工作日）
5. 检测触发事件类型（签订/生效/验收/到货/发票/质保）
6. 去重 + 按总分比例裁切（超过合同总金额的冗余条目被过滤）
7. 置信度标记：高（金额+条件都有）、中（仅金额）、低（仅关键词）

**触发事件映射**:

| 关键词 | 事件类型 |
|--------|----------|
| 签订 | 合同签订 |
| 生效 | 合同生效 |
| 发票/开票 | 收到发票 |
| 验收 | 验收合格 |
| 货到/到货 | 到货 |
| 发货 | 发货 |
| 交付 | 交付 |
| 质保 | 质保期满 |

#### 3.5.3 付款计划管理

- **手动编辑**: 可对自动提取的结果进行增删改
- **确认操作**: 逐条确认 / 一键确认（批量确认满足条件的待确认条目）
- **筛选条件**: 按确认状态、付款状态、日期范围筛选
- **统计视图**: 应付总额、已付总额、未付总额、待确认数量
- **下月付款计划**: 自动计算下月日期范围，查询已确认未付清的到期款项
- **即将到期提醒**: 首页展示 7 天内到期的付款计划

#### 3.5.4 付款计划 Excel 导出

- 按所选报表月份导出，工作簿包含项目汇总页和各项目付款明细页
- 明细页按“项目名称 + 合同内编号”成行，横向展开合同的全部付款节点，包括待确认、已确认、已付、未付、过去及未来节点
- 未关联合同内编号的付款节点以“待补编号”行保留，便于复核补充
- 所选月份仍未付的节点金额、付款条件及该行应付汇总框使用黄色背景；已付款节点使用绿色背景
- 汇总页分别统计所选月份计划、以前月份未付余额和可用银行承兑支付金额

### 3.6 系统设置模块

#### 3.6.1 自启动管理
- 仅 Windows 支持（任务计划程序）
- 支持启用/禁用

#### 3.6.2 诊断页面
- Python 版本、平台信息、路径配置
- 模板/合同/备份数量统计
- 最近 40 行日志预览
- 一键打开相关目录

#### 3.6.3 数据库备份管理
- 每日自动备份（SQLite backup API，原子操作）
- 保留最近 7 天备份
- 手动创建备份（自定义标签）
- 备份下载和恢复（恢复前自动备份当前数据库）
- 完整性校验（quick_check / integrity_check）

---

## 4. 数据模型

### 4.1 数据库 ER 图

```
contracts (1) ---- (N) payment_plans
contracts (1) ---- (N) contract_history
```

### 4.2 数据库索引

| 索引名 | 表 | 列 | 用途 |
|--------|-----|-----|------|
| idx_contracts_created | contracts | created_at DESC | 台账列表排序 |
| idx_payment_contract | payment_plans | contract_id | 合同详情查询付款计划 |
| idx_payment_due | payment_plans | confirm_status, payment_status, due_date | 付款计划筛选查询 |
| idx_history_contract | contract_history | contract_id, changed_at DESC | 变更历史查询 |

### 4.3 文件存储结构

```
合同生成工具/
  templates/         # 模板定义文件 (*.contract-template JSON)
    versions/        # 历史版本备份
  uploads/           # 上传的 DOCX 源文件 (uuid.docx)
  output/            # 生成的合同文档 (.docx/.pdf/.xlsx)
  sessions/          # 会话数据 (*.json)
  data/
    contracts.db     # SQLite 主数据库 (WAL 模式)
    backups/         # 数据库备份 (*.db)
  logs/              # 应用日志
  config.json        # 配置文件
```

---

## 5. 接口规范

### 5.1 路由总览

#### 模板路由

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/create-template` | 模板创建页面 |
| POST | `/template/upload-style` | 上传 DOCX 样式文件 |
| POST | `/template/manual-save` | 保存模板定义 |
| GET | `/templates` | 模板列表页 |
| GET | `/template/<name>` | 模板编辑页 |
| POST | `/template/<name>/preview` | 生成预览文档 |
| POST | `/template/<filename>/delete` | 删除模板 |
| GET | `/template/<name>/versions` | 版本历史页 |
| POST | `/template/<name>/versions/<v>/restore` | 回滚版本 |
| POST | `/template-defaults` | 保存模板预制值 |

#### 合同路由

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/` | 首页（仪表盘） |
| GET | `/editor` | 合同生成编辑器页 |
| POST | `/generate` | 单份合同生成 |
| POST | `/generate-batch` | 批量合同生成（返回 ZIP） |
| GET | `/contracts` | 合同台账列表 |
| GET | `/contracts/export` | 台账导出 Excel |
| POST | `/contracts/batch-delete` | 批量删除合同 |
| POST | `/contracts/batch-status` | 批量变更状态 |
| GET | `/contracts/<id>` | 合同详情页 |
| POST | `/contracts/<id>/update` | 更新合同信息 |
| GET | `/contracts/<id>/download` | 下载合同 DOCX |

#### 付款路由

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/contracts/<id>/payments/save` | 保存付款计划 |
| POST | `/contracts/<id>/payments/confirm-all` | 一键确认全部待确认 |
| GET | `/payment-plans` | 付款计划总览列表 |
| GET | `/payment-plans/export-next-month` | 导出下月付款计划 |
| GET | `/api/payments/due-soon` | 即将到期付款 API (JSON) |

#### 系统路由

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/autostart/enable` | 启用自启动 |
| POST | `/autostart/disable` | 禁用自启动 |
| GET | `/diagnostics` | 系统诊断页 |
| GET | `/api/diagnostics` | 系统诊断 API (JSON) |
| POST | `/diagnostics/open-folder` | 打开文件夹 |
| GET | `/backups` | 备份列表页 |
| POST | `/backups/create` | 创建手动备份 |
| POST | `/backups/<name>/restore` | 恢复备份 |
| GET | `/backups/<name>/download` | 下载备份 |
| GET | `/reset` | 重置会话 |

### 5.2 中间件

| 中间件 | 触发条件 | 行为 |
|--------|----------|------|
| CSRF 校验 | POST/PUT/PATCH/DELETE 请求 | 验证表单 csrf_token 与会话 token 是否一致（hmac 常量时间比较） |
| 双层速率限制 | 所有 POST/PUT/PATCH/DELETE 请求 | 1) 全局 IP 限流 2) 路径维度限流 |

### 5.3 速率限制配置

| 接口 | 限制 (次数 / 60s) |
|------|-------------------|
| `/generate` | 10 |
| `/generate-batch` | 5 |
| `/template/upload-style` | 20 |
| `其他接口（默认）` | 30 |
| `全局（非本地IP）` | 120 |
| `全局（本地IP）` | 1000 |

---

## 6. 安全设计

### 6.1 防护措施总览

| 措施 | 实现方式 |
|------|----------|
| **CSRF 防护** | 所有 POST 请求携带 session _csrf_token，hmac 常量时间比较 |
| **路径遍历防护** | path_within() 检查文件路径是否在允许的根目录内 |
| **文件名校验** | safe_join_file() 拒绝绝对路径、.. 穿越、非允许扩展名 |
| **Session 安全** | SESSION_COOKIE_SAMESITE=Strict, SESSION_COOKIE_HTTPONLY=True |
| **SecretKey 管理** | 随机 32 字节 hex，持久化到 .secret_key，环境变量可覆盖 |
| **公式安全** | AST 白名单求值，仅允许四则运算和 5 个聚合函数 |
| **输入限制** | 文本 <= 10000 字符、字段 <= 200、列 <= 50、行 <= 500、批量 <= 100 |
| **文件上传校验** | ZIP 魔数校验；大小限制 50MB |
| **SQL 注入防护** | 全部参数化查询（? 占位符），CHECK 约束二次校验 |
| **速率限制** | 双层 LRU 内存限流器（IP 全局 + 路径维度），最多 10000 条目后淘汰 |
| **数据库完整性** | 自动备份前 integrity_check，恢复前 quick_check |
| **数据库原子备份** | SQLite backup API 而非直接文件复制 |
| **日志安全** | 不记录密码/token，异常日志保护敏感路径信息 |

### 6.2 安全常量定义

| 常量 | 值 | 说明 |
|------|-----|------|
| `MAX_TEXT_VALUE_LENGTH` | 10000 | 文本字段最大长度 |
| `MAX_TEMPLATE_FIELDS` | 200 | 模板最大字段数 |
| `MAX_TABLE_COLUMNS` | 50 | 表格最大列数 |
| `MAX_TABLE_ROWS` | 500 | 表格最大行数 |
| `MAX_BATCH_CONTRACTS` | 100 | 批量生成最大份数 |
| `MAX_PLAN_ROWS` | 300 | 付款计划最大行数 |
| `MAX_COUNTERPARTY_LENGTH` | 120 | 对方单位名称最大长度 |
| `MAX_FORMULA_LENGTH` | 500 | 公式最大长度 |
| `MAX_AST_NODES` | 100 | AST 最大节点数 |
| `MAX_ABS_NUMBER` | 10^12 | 数值最大绝对值 |

---

## 7. 部署与运行

### 7.1 系统要求

| 项目 | 要求 |
|------|------|
| Python | 3.10+ |
| 操作系统 | Windows 10/11（自启动仅 Windows） |
| Microsoft Word 或 WPS | 可选（仅用于旧版 `.doc` 模板转换） |
| 磁盘空间 | >= 200MB（含依赖） |

### 7.2 安装

```bash
git clone <仓库地址>
cd 合同生成工具
pip install -r requirements.txt
```

### 7.3 启动

```bash
python app.py                  # 正常启动（自动打开浏览器）
python app.py --no-browser     # 静默模式
python app.py --port 8080      # 自定义端口
python app.py --host 0.0.0.0   # 自定义监听地址
```

### 7.4 配置

配置文件 `config.json`（优先级最高），支持环境变量 `CT_*` 覆盖：

| 配置项 | 环境变量 | 默认值 | 说明 |
|--------|----------|--------|------|
| HOST | CT_HOST | 127.0.0.1 | 监听地址 |
| PORT | CT_PORT | 5000 | 监听端口 |
| DEBUG | CT_DEBUG | false | 调试模式 |
| MAX_CONTENT_LENGTH_MB | CT_MAX_CONTENT_LENGTH_MB | 50 | 上传文件大小(MB) |
| CLEANUP_DAYS | CT_CLEANUP_DAYS | 7 | 文件自动清理天数 |
| LOG_LEVEL | CT_LOG_LEVEL | INFO | 日志级别 |

### 7.5 文件清理策略

- 定时清理 `uploads/`、`output/` 中超过 `CLEANUP_DAYS`（默认 7 天）的文件
- 台账引用的 DOCX 路径和模板引用的上传源文件受保护，不被清理
- `sessions/` 不在自动清理范围
- 数据库不可用时跳过清理以避免数据丢失

---

## 8. 非功能需求

### 8.1 性能指标

| 指标 | 目标 |
|------|------|
| 单份合同生成 | < 3 秒 |
| 批量 100 份生成 | < 60 秒 |
| 台账列表（1000条） | < 1 秒 |
| 付款计划提取 | < 5 秒 |
| 启动时间 | < 3 秒 |

### 8.2 可靠性

- **数据库**: WAL 模式支持并发读写；每日自动备份；备份前完整性校验
- **崩溃恢复**: 启动时自动运行数据库迁移；WAL checkpoint 防数据丢失
- **优雅关闭**: SIGINT/SIGTERM 信号处理，关闭前 WAL checkpoint
- **异常处理**: 全局 Flask error handler 覆盖 400/404/429/500 及未捕获异常
- **递归保护**: error handler 渲染出错时降级为纯文本响应

### 8.3 可维护性

- **模块化**: 业务逻辑按功能拆分为独立模块
- **路由分层**: 4 个蓝图各自独立注册
- **配置文件**: JSON 格式，支持注释，优先级链明确
- **日志系统**: 统一格式，RotatingFileHandler 自动轮转（10MB/文件，保留3个备份），分级输出
- **数据库迁移**: 版本化迁移机制，支持前向/回滚
- **版本管理**: 模板历史版本自动备份，支持回滚

### 8.4 可用性

- 启动后 1s 内自动打开浏览器
- 可视化模板配置：上传 -> 检测 -> 配置 -> 保存，全流程 Web UI
- 预制值功能：模板可预设默认值和表格数据
- 实时预览：模板预览功能
- 状态标签中文化：所有枚举状态均有中文映射

### 8.5 兼容性

- **浏览器**: Chrome/Firefox/Edge
- **Word**: 2013/2016/2019/365
- **打包**: 支持 PyInstaller 打包为独立 exe

---

## 9. 附录

### 9.1 依赖清单

```
flask>=3.0,<4.0          # Web 框架
python-docx>=1.0,<2.0    # DOCX 读写
openpyxl>=3.1,<4.0       # XLSX 读写
pywin32>=300             # Windows COM（PDF导出）
```

### 9.2 项目结构

```
合同生成工具/
  app.py                    # Flask 主程序（应用工厂、中间件、路由注册）
  config.py                 # 配置管理（config.json + 环境变量）
  config.json               # 运行时配置文件
  docx_builder.py           # DOCX 文档写入引擎
  template_def.py           # 模板定义管理（CRUD + 版本管理）
  field_eval.py             # 公式安全求值引擎
  ledger_store.py           # SQLite 台账存储（CRUD + 备份/恢复/迁移）
  payment_extractor.py      # 付款条款规则提取引擎
  xlsx_exporter.py          # Excel 导出（付款计划 + 合同台账）
  demo_flow_data.py         # 演示数据生成
  pyproject.toml            # pytest 配置
  requirements.txt          # Python 依赖
  .secret_key               # Flask Secret Key（自动生成）
  routes/                   # 路由蓝图
    __init__.py             # 蓝图注册
    templates_bp.py         # 模板管理路由
    contracts_bp.py         # 合同生成和台账路由
    payments_bp.py          # 付款计划路由
    settings_bp.py          # 系统设置路由
  utils/                    # 工具模块
    constants.py            # 枚举常量（FieldType, ContractStatus 等）
    security.py             # 安全校验（CSRF, 路径遍历, 输入限制）
    helpers.py              # 会话管理、路径工具、模块重组口
    field_utils.py          # 字段解析、占位符检测、值解析
    generation_utils.py     # 合同生成、计算、台账、批量处理
    labels.py               # 状态标签中文映射
    logger.py               # 统一日志配置
    errors.py               # JSON/HTML 错误响应
    autostart.py            # Windows 自启动管理
  templates/                # Jinja2 HTML 模板 + *.contract-template
  static/                   # 前端静态资源
  data/                     # SQLite 数据库 + 备份
  uploads/                  # 上传的 DOCX 文件
  output/                   # 生成的合同文档
  sessions/                 # 会话 JSON 文件
  logs/                     # 应用日志
  tests/                    # 测试代码
  build_exe.py              # 打包为 exe
  build_package.py          # app/installer/desktop 构建统一入口
  build_release.py          # 构建发布版本
  install.bat               # 安装脚本（批处理）
  install.ps1               # 安装脚本（PowerShell）
  setup_autostart.ps1       # 设置自启动
  setup_autostart_remove.ps1 # 移除自启动
  启动合同生成工具.bat        # 一键启动
```

### 9.3 术语表

| 术语 | 说明 |
|------|------|
| 模板 (Template) | .contract-template JSON 文件，定义合同结构和字段 |
| 样式文档 (Source DOCX) | 上传的参考 DOCX 文件，含占位符和格式 |
| 字段 (Field) | 模板中的可填充单元，含 key/label/type/location |
| 占位符 (Placeholder) | DOCX 中的 {字段名} 标记 |
| 台账 (Ledger) | contracts 表中的合同记录 |
| 付款计划 (Payment Plan) | payment_plans 表中的付款条款记录 |
| 计算字段 (Calculated Field) | 通过公式自动求值的字段 |
| 置信度 (Confidence) | 付款计划自动提取的可信度标记 |
| WAL | Write-Ahead Logging，SQLite 并发写入机制 |
| CSRF | Cross-Site Request Forgery，跨站请求伪造 |

---

> **文档维护**: 本文档随代码变更同步更新。重大功能变更后需修订对应章节。
