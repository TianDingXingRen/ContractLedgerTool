# 变更日志

本项目遵循语义化版本。发布标签、`version.txt` 和安装包内版本必须一致。

## 1.3.0 - 2026-07-22

### 新功能

- 合同详情升级为可深链接的业务工作区，提供概览、付款管理、投产管理、发票管理和变更记录五个标签。
- 合同、付款规则、付款计划和付款登记统一使用原生右侧抽屉，支持 Esc、点击遮罩关闭、焦点恢复、未保存提醒和重复提交防护。
- 合同概览新增合同金额、累计投产、已确认应付、未付余额、有效发票和待分摊金额汇总，并分开展示待处理事项和真实业务动态。

### 改进

- 付款计划和全局付款工作台改为只读业务表，移除行内日期、金额编辑和列宽拖动；确认、全额付款、部分付款与完整编辑均可快速完成。
- 投产通知与发票台账新增随筛选同步的汇总带，统一筛选、分页、空状态、状态徽标、金额和日期格式。
- 合同详情按当前标签加载明细，付款、投产和发票使用独立分页参数；表单返回参数采用标签、分页和锚点白名单校验。
- 建立共享指标带、状态徽标、金额格式、空状态、分页和抽屉组件，桌面端统一为单一蓝色主操作与语义状态色。

### 质量

- 新增合同工作区路由、汇总口径、只读表格和抽屉可访问性测试，并回归规则修正、动态投产付款、部分付款、发票分摊和红冲流程。
- 在 1280×800、1440×900、1920×1080、明暗主题和 125% 缩放下完成浏览器布局验证，页面无横向溢出。
- 完整测试共 482 项，CSS 生产构建与 JavaScript 语法检查通过。

## 1.2.0 - 2026-07-22

### 新功能

- 新增完全离线的付款条款规则抽取与复核，覆盖固定节点、条件付款、比例付款和“每次投产通知按通知内产品总价一定比例付款”等重复触发条款。
- 新增付款规则和业务事件台账；投产通知发出后，可按通知金额自动生成对应的动态付款计划。
- 新增合同产品基线与号段维护，支持登记投产通知、修订版本、供应商收悉、关闭、取消以及累计投产数量统计。
- 新增发票管理，支持发票附件、核验、抵扣、作废、全额红冲，以及按合同、投产通知和付款计划进行金额分摊。

### 改进

- 付款抽取增加文本归一化、条款切分、规则优先级、歧义原因码和人工确认状态，不依赖大模型或外部网络。
- 投产通知、付款事件和发票分摊增加同源校验、累计额度校验、单一生效版本和并发数据库约束。
- 未付款的动态付款计划在投产通知金额变化时自动重算；已付款或已作废计划阻止不一致修改。
- 合同产品增加号段长度校验、删除态保护、变更审计和采购同步异常留痕。
- 投产通知和发票台账增加分页，发票分摊目标改为按合同动态加载。
- 完整数据备份纳入发票附件，附件删除增加文件与数据库失败补偿。

### 修复

- 修复多个投产通知修订版本可同时生效、手工事件编号碰撞以及付款计划金额未同步的问题。
- 修复发票跨合同/跨事件错误分摊、累计分摊超限、部分分摊发票被误标核验通过等问题。
- 修复作废或红冲发票仍占用投产通知和付款计划额度的问题。

### 质量

- 拆分付款、投产通知和发票路由注册职责，收敛仓储门面与复杂业务函数，全部通过架构预算。
- 新增付款规则、投产通知、发票状态机、数据库迁移、备份恢复和页面回归测试；完整测试共 478 项。

## 1.1.0 - 2026-07-20

### 新功能

- 增加完整浏览器端到端回归，覆盖模板选择、字段填写、生成前复核、DOCX 下载、合同详情与台账入账。
- 增加 Windows EFS 本地数据保护，可选择加密数据库、备份、模板、上传件、生成件、填写会话及本地密钥。
- 增加 Office 兼容性检查脚本与机器可读报告，支持按需调用真实 Word/LibreOffice 转换器。

### 改进

- 建立跨 run 占位符、合并表头、重复明细行、页眉页脚、多节文档和中英文符号的 DOCX 兼容性矩阵。
- 扩充生成前检查、PDF 转换和采购文档下载路由的异常路径测试。
- 全量覆盖率提升至 73.76%，持续集成最低覆盖率门槛由 70% 上调至 72%。
- 端到端及文档测试改用隔离运行目录和动态测试模板，不再依赖本地样例文件或用户数据。
- 数据库与完整数据包恢复改为独占维护、原子替换和初始化失败自动回滚。
- DOC/PDF 转换改为独立 Office 子进程，强制禁用宏；Word 默认使用可配置的 60 秒硬超时。
- 公式计算改用十进制运算和财务四舍五入，浏览器预览与生成文档统一保留指定小数位。
- 性能预算测试与 coverage 插桩分离，并为关键模块增加独立覆盖率下限。
- 离线安装器增加 Windows 版本信息、标准卸载入口和保留业务数据的卸载策略，开机自启动改为显式启用。
- 诊断页显示命令行实际监听端口，并统一中文面包屑、文件上传文案、表单可访问名称和中等屏幕编辑顺序。

### 安全

- EFS 启用前强制确认私钥恢复风险，并在诊断页持续展示支持状态、加密覆盖和部分失败信息。
- 本地数据加密保持默认关闭，避免在用户尚未备份 EFS 证书时自动锁定既有数据。
- 模板、采购 Excel 和完整备份统一校验内部路径、成员数量、解压体积与压缩比，失败时清理临时文件。
- 局域网访问要求至少16位环境变量令牌；默认本机访问行为不变。
- EFS 部分失败时自动回滚本次加密操作，并报告无法回滚的项目。
- 正式发布强制验证内外层 EXE 的 Authenticode 签名与可信时间戳，缺少签名凭据时中止构建。

## 1.0.0 - 2026-07-17

### 新功能

- 完成采购、比价、合同生成、台账、付款计划和交接导出的单机业务闭环。
- 增加数据库升级前备份、事务迁移、合同生成崩溃恢复和诊断自检。

### 修复

- 安装失败时自动恢复上一版本程序文件，并在提交安装前执行隔离自检。
- 离线发布目录收敛为单一安装包。

### 重构

- 引入应用工厂、Blueprint、运行时服务容器和付款计划 Repository。
- 建立持续集成、覆盖率、架构预算和可重复发布门禁。
