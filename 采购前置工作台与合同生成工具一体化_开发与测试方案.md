# 采购前置工作台与合同生成工具一体化 - 开发与测试方案

> 编制日期：2026-06-23  
> 需求来源：`采购前置工作台与合同生成工具一体化开发方案.pdf`，V3.0，共 27 页  
> 代码基线：当前合同生成工具工作区  
> 建设方式：在现有本地 Flask 应用中增量开发，不另建第二套系统

---

## 1. 结论与关键决策

本项目应建设为同一个本地应用中的两条连续业务链：

```mermaid
flowchart LR
    A["采购项目"] --> B["采购明细与候选供应商"]
    B --> C["标准报价模板"]
    C --> D["供应商报价导入"]
    D --> E["横向比价与异常提示"]
    E --> F["澄清与多轮报价"]
    F --> G["成交建议确认"]
    G --> H["合同数据单"]
    H --> I["现有合同生成、台账与付款计划"]
```

首版必须优先打通“项目建档 -> 标准报价 -> 比价 -> 成交确认 -> 合同数据单 -> 合同生成”的最短闭环。澄清问题和成交建议初稿纳入首版，多轮谈判深度能力、非标准文件解析和历史价格策略放在后续阶段。

关键工程决策如下：

1. 保留现有合同模板、DOCX 生成、合同台账、付款计划、Excel 单据、备份和诊断能力。
2. 新增真正的 `procurement_project` 领域对象；当前合同表中的 `project_name` 只是分类标签，不能充当采购项目主数据。
3. 标准报价 Excel 是首版唯一自动导入格式；非标准 Excel 使用人工列映射，Word 表格导入后置，扫描 PDF/OCR 不进入首版。
4. 比价和异常识别只提供规则结果及依据，不自动决定成交供应商。
5. “一键转合同”先生成可复核、可版本化的合同数据单，再预填现有合同编辑器；不能绕过人工确认直接形成已签订合同。
6. 转合同必须幂等：重复点击不得重复生成台账记录；失败时不得留下半成品关联。
7. 金额不得使用二进制浮点参与业务计算。新增采购表使用最小货币单位整数或规范化十进制字符串，服务层统一使用 `Decimal`。
8. 原始报价文件不可覆盖；解析数据、比价结果、人工修改、成交确认和转合同操作均需留痕。

---

## 2. 当前代码基线与复用判断

当前系统已具备较好的下游合同能力。2026-06-23 最近一次完整自动化回归为 `181 passed, 5 skipped`。新增采购模块前必须保留这组回归作为兼容门禁。

| 当前能力 | 现有实现 | 一体化方案中的处理 |
|---|---|---|
| 应用工厂与路径 | `app.create_app()`、`RuntimePaths` | 直接复用；增加采购项目目录属性 |
| 路由注册 | `routes.register_all()` | 沿用 register 模式，新增采购路由模块 |
| 模板定义 | `template_def.TemplateDef` | 复用字段、表格、公式和版本；增加文档用途元数据 |
| DOCX 生成 | `docx_builder.py`、`generation_utils.py` | 复用生成链路，增加采购类模板上下文 |
| Excel 生成 | `openpyxl`、`excel_bill_service.py`、`xlsx_exporter.py` | 复用样式/导出经验；报价导入使用独立服务 |
| 合同台账 | `ledger_store` | 保留；通过关联表连接采购项目，避免强耦合 |
| 付款计划 | `payment_extractor.py`、`payment_plans` | 合同生成后继续使用；成交付款条件可作为预填来源 |
| 数据迁移 | `schema_version`、v2-v8 迁移 | 先增强迁移执行器，再新增采购表迁移 |
| 备份恢复 | SQLite 备份与 WAL 处理 | 新表使用同一数据库，自动包含在备份中 |
| 前端 | Jinja2 + 原生 JS + Alpine.js | 沿用现有设计系统，侧边栏新增“采购前置”分组 |
| 测试 | pytest、Flask test client、Node 语法检查 | 保留全部回归，新增采购领域分层测试 |

当前主要缺口：

- 没有采购项目、采购明细和项目内候选供应商实体。
- 没有报价导入暂存、预览、确认和重复导入控制。
- 没有报价行与项目明细的稳定关联键。
- 没有比价运行批次、异常规则版本和人工确认结果。
- 没有澄清、多轮报价、成交建议和项目文件台账。
- 没有采购结果到合同模板字段的映射配置。
- 没有采购项目到合同记录的幂等关联及转换历史。
- 当前 Git 工作区已有大量未提交修改和未跟踪文件；正式开发前需要先建立可回退基线。

---

## 3. 产品范围

### 3.1 首版范围

- 采购项目 CRUD、归档和状态管理。
- 采购明细维护、Excel 粘贴/导入和导出。
- 项目内候选供应商管理。
- 从项目生成询价函和标准报价模板。
- 标准报价 Excel 的上传、校验、预览、确认导入。
- 多供应商横向比价页面及 Excel 导出。
- 漏项、高低价、交期、付款、税率、质保和偏离提示。
- 从异常结果生成澄清问题初稿并支持人工编辑。
- 成交供应商、成交明细和成交建议确认。
- 生成合同数据单并预填现有合同编辑器。
- 合同生成成功后建立采购项目、成交建议、合同台账三者关联。
- 原始文件、生成文件、解析数据和关键操作留痕。

### 3.2 后续范围

- 非标准 Excel 的字段映射导入。
- Word 报价表格的人工选择与解析。
- 多轮报价完整过程、降价幅度和承诺变化分析。
- 谈判纪要 Word、澄清函 Word、成交建议 Word 的高级模板。
- 历史成交价、历史报价和同供应商让利分析。
- 本地或内网大模型辅助起草，且必须人工复核。

### 3.3 明确不做

- ERP/SRM 替代、ERP 自动写入。
- 供应商在线门户、多用户权限和审批流。
- 自动确定成交供应商、自动承诺价格/交期/付款条件。
- 自动签章、复杂法务审查。
- 扫描 PDF/OCR 和公网大模型调用。

---

## 4. 业务流程与状态设计

### 4.1 采购项目状态

| 状态 | 允许进入条件 | 主要允许操作 |
|---|---|---|
| `draft` 草稿 | 项目已创建 | 编辑项目、明细和供应商 |
| `documents_ready` 询价资料已生成 | 至少一条明细 | 下载/重新生成询价资料 |
| `inquiry_sent` 已发出询价 | 至少一个候选供应商 | 记录发出时间，导入报价 |
| `quotes_received` 已收到报价 | 至少一份已确认报价 | 比价、追加报价 |
| `clarifying` 澄清中 | 存在已确认澄清问题 | 记录回复、重新比价 |
| `negotiating` 谈判中 | 已开始新报价轮次 | 导入多轮报价、记录承诺 |
| `award_draft` 成交建议草稿 | 存在有效比价结果 | 编辑推荐意见 |
| `award_confirmed` 成交建议已确认 | 推荐供应商、金额和明细完整 | 转合同数据单 |
| `contract_draft` 已生成合同数据单 | 数据单校验通过 | 选择模板、复核字段 |
| `contract_created` 已生成合同 | 已获得合同台账 ID | 查看合同、输出归档资料 |
| `archived` 已归档 | 项目闭环 | 只读查看、恢复归档 |

状态跳转由服务层统一校验，不允许路由直接写状态。回退到前一状态必须记录原因；已转合同后修改成交数据需要创建新版本，不能静默覆盖。

### 4.2 报价导入流程

```text
上传文件
  -> 校验格式与模板版本
  -> 计算文件 SHA-256
  -> 解析到暂存对象
  -> 校验项目、供应商、轮次、单位、数量、税率和金额
  -> 展示错误/警告及解析预览
  -> 用户确认
  -> 单事务写入报价主表和明细表
  -> 原始文件登记为不可覆盖的项目文件
  -> 触发新的比价运行
```

错误会阻止确认；警告允许用户确认但必须保存确认人、时间和说明。相同项目、供应商、轮次及文件哈希不得重复导入。

### 4.3 比价规则

- 以 `project_item_id` 对齐报价明细，不以物资名称模糊匹配作为首版主逻辑。
- 标准模板必须携带稳定的项目明细 ID 和模板格式版本。
- 单价统一为同一含税/不含税口径后才能横向比较；口径不同必须先标记异常。
- 金额计算使用 `Decimal`，分项金额与总金额需进行交叉校验。
- 高低价默认阈值建议为有效报价均值的正负 20%，阈值放入配置并显示计算依据。
- 有效供应商少于 2 家时不产生均值高低价结论。
- 漏项、零价、负数、数量不一致、税率异常和公式单元格均单独提示。
- 每次比价形成不可变的 `comparison_run`；规则或报价改变后创建新批次，不覆盖历史结果。

### 4.4 成交与转合同

成交建议必须由采购人员明确选择供应商和成交明细。若未选择最低有效总价供应商，必须填写原因。

转合同使用标准中间对象，不让采购服务直接依赖 HTML 表单字段：

```json
{
  "schema_version": "1.0",
  "project_id": 1,
  "project_no": "CG-20260623-0001",
  "project_name": "结构件加工竞争性谈判",
  "award_recommendation_id": 1,
  "supplier": {"id": 3, "name": "示例供应商"},
  "currency": "CNY",
  "amount_minor": 12800000,
  "items": [],
  "delivery_place": "",
  "delivery_terms": "",
  "payment_terms": "",
  "technical_notes": "",
  "commercial_notes": "",
  "contract_notices": [],
  "source_quote_ids": [7]
}
```

合同模板通过“映射配置”把该标准对象映射到现有模板字段。生成合同数据单后打开现有编辑器进行人工复核；合同台账保存成功后，`project_contract_links` 才标记为完成。

---

## 5. 技术架构

### 5.1 建议目录

```text
routes/
  procurement_projects_bp.py
  quote_import_bp.py
  quote_compare_bp.py
  clarification_bp.py
  negotiation_bp.py
  award_recommendation_bp.py
  project_to_contract_bp.py

services/
  procurement_project_service.py
  project_document_service.py
  quote_template_service.py
  quote_import_service.py
  quote_compare_service.py
  clarification_service.py
  award_recommendation_service.py
  contract_data_sheet_service.py
  project_to_contract_service.py

procurement_store/
  migrations.py
  projects.py
  quotes.py
  comparisons.py
  clarifications.py
  awards.py
  files.py

templates/procurement/
  project_list.html
  project_form.html
  project_detail.html
  quote_import.html
  quote_compare.html
  clarification_list.html
  award_recommendation.html
  project_to_contract.html

static/js/procurement/
  project-items.js
  quote-import.js
  quote-compare.js
  award-recommendation.js
```

路由仅处理 HTTP 输入输出；业务规则、事务和状态转换放在 `services`；SQL 放在 `procurement_store`。不得把所有新增 SQL 继续堆入现有 `ledger_store/__init__.py`。

### 5.2 运行时文件目录

`RuntimePaths` 增加 `procurement_dir`，建议位于：

```text
output/procurement/{project_no}_{safe_name}/
  01_谈判文件/
  02_询价函/
  03_报价模板/
  04_供应商报价/
  05_横向比价/
  06_澄清文件/
  07_谈判纪要/
  08_成交建议/
  09_合同资料/
  10_归档包/
```

数据库只保存相对运行时根目录的路径，并在 `project_files` 中保存文件类型、原始文件名、SHA-256、大小、MIME、版本和创建时间。目录名变化不应破坏数据库引用。

### 5.3 模板扩展

在兼容现有 `.contract-template` 格式的前提下增加可选元数据：

- `document_type`：`contract`、`inquiry_letter`、`quote_template`、`clarification_letter`、`negotiation_minutes`、`award_recommendation`。
- `context_schema`：模板期望的数据对象版本。
- `mapping_profile`：标准字段到模板字段的映射。
- `output_folder_type`：对应项目目录类别。

不为询价函、成交建议等重新实现 DOCX 引擎。

---

## 6. 数据模型

PDF 中的基础表可保留，但首版应补齐约束、版本、文件和转换追踪。

### 6.1 核心表

| 表 | 作用 | 关键约束/索引 |
|---|---|---|
| `procurement_projects` | 采购项目主表 | `project_no` 非空唯一；状态检查；软归档 |
| `project_items` | 项目采购明细 | `(project_id, line_no)` 唯一；项目外键 |
| `project_suppliers` | 项目候选供应商 | `(project_id, normalized_name)` 唯一 |
| `project_files` | 原始及生成文件登记 | `(project_id, file_type, version)`；文件哈希索引 |
| `supplier_quotes` | 每家每轮报价主表 | `(project_id, supplier_id, quote_round)` 唯一 |
| `supplier_quote_items` | 报价明细 | `(quote_id, project_item_id)` 唯一 |
| `quote_import_jobs` | 导入暂存、错误和确认结果 | 文件哈希、状态、解析器版本 |
| `comparison_runs` | 一次比价运行 | 项目、报价版本集合、规则版本、运行时间 |
| `comparison_results` | 漏项/异常/偏离结果 | 运行批次、供应商、项目明细、严重度、确认状态 |
| `clarification_questions` | 澄清问题及回复 | 来源结果、供应商、状态、版本 |
| `negotiation_rounds` | 谈判轮次头及过程记录 | `(project_id, round_no)` 唯一 |
| `award_recommendations` | 成交建议版本 | 项目、版本、推荐供应商、状态 |
| `award_recommendation_items` | 成交明细快照 | 建议版本、项目明细、来源报价、成交价 |
| `contract_data_sheets` | 转合同标准中间对象 | 项目、建议版本、JSON 模式版本、状态 |
| `project_contract_links` | 项目与合同台账关联 | `data_sheet_id` 唯一；`contract_id` 唯一 |
| `procurement_audit_events` | 关键操作审计 | 实体类型/ID、动作、前后摘要、时间 |

### 6.2 字段修正建议

- 新采购金额使用 `amount_minor INTEGER` 和 `currency TEXT`，不使用 `REAL`。
- 税率使用基点整数，例如 13% 存为 `1300`；同时保存含税/不含税口径。
- 数量使用规范化十进制字符串或受控 NUMERIC，服务层统一 `Decimal`。
- 报价明细必须保存项目明细 ID；名称、规格等作为导入时快照，便于审计。
- 成交建议保存明细快照，后续项目明细变化不能改写已确认建议。
- 所有业务表包含 `created_at`、`updated_at`；需要版本控制的表增加 `version`。
- 外键明确 `ON DELETE RESTRICT` 或 `CASCADE`，不得依赖默认行为。

### 6.3 迁移策略

1. 迁移前自动执行 SQLite 在线备份，并记录备份路径。
2. 增强现有迁移执行器以支持多语句/回调迁移及事务失败回滚。
3. 从当前最高版本顺序新增采购表，不改写历史迁移。
4. 新库初始化与历史数据库升级走同一套迁移结果。
5. 迁移重复执行必须幂等；部分失败后 `schema_version` 不前进。
6. 启动后执行外键检查、必要索引检查和轻量完整性检查。

---

## 7. 标准报价 Excel 设计

首版标准工作簿建议包含：

| Sheet | 内容 |
|---|---|
| `填报说明` | 版本、填写规则、税价口径、禁止修改项 |
| `报价信息` | 项目编号、供应商、轮次、日期、有效期、税率、交期、付款、质保 |
| `报价明细` | 稳定明细 ID、物资、规格、图号、数量、单位、单价、金额、偏离、备注 |

要求：

- 工作簿内写入不可见或受保护的 `format_version`、`project_id`、`project_item_id`。
- 用户输入列与系统列用样式区分，关键列使用数据验证。
- 金额公式只用于用户体验；导入后由服务端重新计算并校验，不能直接相信缓存值。
- 只接受 `.xlsx`；宏、外部链接、错误公式、缺失版本和项目不匹配均拒绝或要求人工确认。
- 解析完成先展示预览，用户确认前不写正式报价表。

---

## 8. 接口与页面规划

首版页面：

1. 采购项目列表：状态、经办人、更新时间、报价进度、快捷动作。
2. 项目详情：基本信息、明细、供应商、文件、报价、比价、澄清、成交和合同关联的分区视图。
3. 报价导入：上传、解析进度、错误/警告、逐行预览、确认。
4. 横向比价：固定项目列、供应商分组列、异常标识、口径说明和导出。
5. 澄清问题：按供应商分组、来源追踪、批量确认、回复和关闭。
6. 成交建议：选择供应商及明细、金额校验、理由和风险说明。
7. 转合同：选择合同模板、查看映射结果、修正必填缺口、进入合同编辑器。

重要服务接口：

- `create_project()` / `update_project()` / `transition_project_status()`
- `generate_quote_template(project_id)`
- `parse_quote(file, project_id, supplier_id, round_no)`
- `confirm_quote_import(import_job_id)`
- `run_comparison(project_id, quote_ids, rule_config)`
- `generate_clarifications(comparison_run_id)`
- `create_award_recommendation(project_id, selection)`
- `confirm_award_recommendation(recommendation_id)`
- `build_contract_data_sheet(recommendation_id)`
- `prefill_contract_editor(data_sheet_id, template_id)`
- `complete_contract_link(data_sheet_id, contract_id)`

---

## 9. 分阶段开发计划

以下为一名熟悉现有代码的全栈工程师估算，不含需求等待和用户验收等待时间。

| 阶段 | 工作内容 | 预计人日 | 退出标准 |
|---|---|---:|---|
| M0 基线与详细设计 | 固化当前工作树、建立分支；确认字段字典、税价口径、状态机、模板样例；迁移方案评审 | 2-3 | 可回退基线、字段/状态/接口评审通过 |
| M1 采购领域基础 | 迁移执行器增强；采购项目、明细、候选供应商、项目文件；页面和审计 | 5-7 | 项目 CRUD、明细和供应商完整可用，备份恢复通过 |
| M2 标准报价链路 | 标准模板生成；上传暂存；解析、预览、确认；原文件登记；重复导入控制 | 7-10 | 三家标准报价可稳定导入，错误不污染正式数据 |
| M3 比价与合同打通 | 比价批次、异常规则、页面/Excel；成交选择；合同数据单；模板映射和幂等转合同 | 8-11 | 最短闭环从项目到合同 DOCX 全程可追溯 |
| M4 澄清与成交文档 | 澄清问题、回复、澄清函；成交建议初稿和 Word；基础多轮报价 | 6-9 | PDF 所列首版 7 个入口均可用 |
| M5 稳定化与发布 | 全量回归、E2E、容量、安装/升级/恢复、用户手册和验收 | 5-7 | 达到发布门禁，无 P0/P1 缺陷 |

首版总量约 33-47 人日，建议 7-10 周完成。若两人并行，服务/数据与页面/文档可并行，但数据库迁移、报价格式和转合同接口必须先完成联合设计。

后续迭代：

- 非标准 Excel 和 Word 导入：5-8 人日。
- 完整多轮报价、谈判纪要：4-7 人日。
- 历史价格与策略辅助：6-10 人日。
- 本地模型辅助起草：另行评估模型、硬件、脱敏和人工复核流程。

---

## 10. 测试总体方案

### 10.1 测试分层

| 层级 | 目标 | 主要工具 |
|---|---|---|
| 单元测试 | 状态机、Decimal、比价规则、异常阈值、映射和文件命名 | pytest |
| 数据库测试 | 新库、v8 升级、幂等迁移、约束、回滚和级联规则 | pytest + 临时 SQLite |
| 解析契约测试 | 标准 Excel 版本、表头、类型、公式和错误定位 | pytest + openpyxl + 固定样例 |
| 服务集成测试 | 导入确认事务、比价批次、成交快照、转合同幂等 | pytest |
| 路由集成测试 | Flask 页面、CSRF、状态码、分页、上传和下载 | Flask test client |
| 产物测试 | XLSX/DOCX/ZIP 的数据、结构、公式、样式和可打开性 | openpyxl、python-docx |
| 浏览器 E2E | 7 个首版入口和完整最小闭环 | Playwright + Edge/Chrome |
| 安装/升级测试 | 源码、单 EXE、离线安装、覆盖升级、备份恢复 | Windows 10/11 实机或虚拟机 |
| 非功能测试 | 大项目、并发点击、故障恢复、磁盘占用和响应时间 | 定制 pytest/脚本 |

### 10.2 测试数据基线

建立一个可重复使用的“结构件加工竞争性谈判”样例：

- 2 个项目明细：不同规格、数量、单位和交期。
- 3 家候选供应商。
- A：完整报价，价格居中。
- B：漏一项，另一项高于均值 25%，付款条件偏离。
- C：完整报价，一项低于均值 25%，存在技术偏离。
- 第二轮报价：A/C 降价，B 补齐漏项并修改交期。
- 成交选择非最低价供应商，用于验证必须填写推荐理由。

固定样例还需覆盖：中文长名称、空白单元格、日期序列值、文本日期、两位/四位小数、0/负数、合并单元格、公式、重复行、错误项目编号、错误模板版本和超大附件。

---

## 11. 核心测试用例矩阵

### 11.1 数据库与项目

| 编号 | 场景 | 预期 |
|---|---|---|
| MIG-01 | 空数据库启动 | 一次创建全部合同和采购表、约束和索引 |
| MIG-02 | 当前 v8 数据库升级 | 原合同、付款和历史数据完全保留 |
| MIG-03 | 重复启动/重复迁移 | 不重复建表、不改变已有数据 |
| MIG-04 | 迁移中途失败 | 版本号不前进，数据库可恢复，升级前备份存在 |
| MIG-05 | 备份恢复 | 采购项目、原始文件登记和合同关联一致 |
| PRJ-01 | 项目编号重复 | 第二次创建明确失败 |
| PRJ-02 | 状态非法跳转 | 服务拒绝且不写审计事件 |
| PRJ-03 | 已有报价后删除明细 | 拒绝删除或要求创建新版本，不产生孤儿报价 |
| PRJ-04 | 项目归档/恢复 | 文件和关联保留，归档状态只读 |

### 11.2 报价模板与导入

| 编号 | 场景 | 预期 |
|---|---|---|
| QTP-01 | 2 项目明细生成模板 | 项目信息、稳定明细 ID、列和数据验证正确 |
| QTP-02 | 无项目明细生成模板 | 明确阻止，不产生空模板文件 |
| QIM-01 | 三家标准报价导入 | 主表/明细正确，原文件登记，状态进入已确认 |
| QIM-02 | 模板版本错误 | 拒绝并指出版本位置 |
| QIM-03 | 项目编号不匹配 | 拒绝写入当前项目 |
| QIM-04 | 金额公式与服务端计算不一致 | 显示错误，确认按钮不可用 |
| QIM-05 | 相同文件重复导入 | 识别哈希并拒绝或指向已有记录 |
| QIM-06 | 同供应商同轮次不同文件 | 必须显式覆盖为新版本或新轮次，不静默覆盖 |
| QIM-07 | 任一明细无效 | 确认事务整体回滚，不留下半份报价 |
| QIM-08 | 1000 行标准报价 | 在性能目标内完成，错误行号准确 |

### 11.3 比价、澄清和谈判

| 编号 | 场景 | 预期 |
|---|---|---|
| CMP-01 | 3 家 2 项完整报价 | 按项目明细 ID 对齐，单价/总价正确 |
| CMP-02 | 某供应商漏项 | 产生 `missing_item`，不把空值当作 0 |
| CMP-03 | 高低价阈值边界 | 20% 边界行为与配置一致，显示公式依据 |
| CMP-04 | 仅一家有效供应商 | 不产生均值高低价结论，提示样本不足 |
| CMP-05 | 含税口径不一致 | 标记税价口径异常，禁止直接比较 |
| CMP-06 | 相同输入重复运行 | 可生成新批次但结果稳定，旧批次不可变 |
| CMP-07 | 比价 Excel | 项目、供应商、异常标记和金额与页面一致 |
| CLR-01 | 从漏项/偏离生成问题 | 问题关联正确的结果、供应商和项目明细 |
| CLR-02 | 编辑、确认、发出、回复、关闭 | 状态合法且全过程留痕 |
| CLR-03 | 比价结果被忽略 | 不再自动生成澄清问题 |
| NEG-01 | 两轮报价 | 轮次唯一，降价额/幅度和承诺变化正确 |

### 11.4 成交与合同集成

| 编号 | 场景 | 预期 |
|---|---|---|
| AWD-01 | 选择最低有效价 | 自动带出明细和金额，仍需人工确认 |
| AWD-02 | 未选择最低价且无说明 | 不允许确认成交建议 |
| AWD-03 | 成交总额与明细和不一致 | 服务端拒绝确认 |
| AWD-04 | 已确认建议后报价变化 | 原建议保持快照，需创建新版本 |
| HND-01 | 转合同数据单 | 项目、供应商、金额、明细、交期和付款正确映射 |
| HND-02 | 合同模板缺少映射 | 显示缺口，不生成错误合同 |
| HND-03 | 重复点击转合同 | 返回已有数据单/合同关联，不重复入账 |
| HND-04 | DOCX 生成失败 | 不创建完成关联，数据单保留可重试状态 |
| HND-05 | 台账写入失败 | 新文件清理或标记失败，不产生悬空关联 |
| HND-06 | 合同生成成功 | 合同可下载，台账可回溯项目、建议和来源报价 |
| HND-07 | 付款条件预填 | 只作为待复核输入，最终付款计划与合同确认值一致 |

### 11.5 文件、回归和发布

| 编号 | 场景 | 预期 |
|---|---|---|
| FILE-01 | 同名项目/供应商/文件 | 目录和版本不冲突，不覆盖原文件 |
| FILE-02 | 移动运行时根目录 | 相对路径仍可解析全部项目文件 |
| FILE-03 | 生成归档包 | 目录完整，清单包含哈希和来源信息 |
| REG-01 | 现有完整测试 | 不低于当前 `181 passed, 5 skipped` 基线 |
| REG-02 | 原合同生成主链路 | 单份、批量、预览、台账和付款计划继续通过 |
| E2E-01 | PDF 最小闭环样例 | 项目到合同 DOCX 全流程成功，所有中间数据可追溯 |
| PKG-01 | 全新安装 | 无 Python 环境可运行，采购目录可写 |
| PKG-02 | 覆盖升级 | 合同和采购数据、模板、原文件、配置均保留 |
| REC-01 | 导入确认时进程中断 | 数据库无半事务，原文件可重新识别 |

---

## 12. 性能与容量目标

| 场景 | 首版目标 |
|---|---:|
| 采购项目列表，1 万项目，分页 20 条 | P95 < 1 秒 |
| 标准报价导入，1000 明细行 | P95 < 5 秒 |
| 横向比价，10 家供应商 x 1000 项 | P95 < 8 秒 |
| 横向比价 Excel 导出 | P95 < 10 秒 |
| 澄清问题生成，500 条异常 | P95 < 3 秒 |
| 合同数据单构建与模板映射 | P95 < 2 秒 |
| 单份合同 DOCX 生成 | 延续现有 < 3 秒目标 |

性能测试记录 P50、P95、最大内存、数据库大小和输出文件大小。规则计算和导入必须在请求超时前结束；超出规模时应显示进度或转为受控后台任务，而不是浏览器无响应。

---

## 13. 发布门禁与验收

首版发布必须同时满足：

1. PDF 规定的 2 明细、3 供应商最小闭环完整通过。
2. 标准报价模板、三家报价、比价表、澄清清单、成交建议、合同数据单和合同 DOCX 均可生成。
3. 每份输出均可回溯项目、供应商、报价轮次和源文件。
4. 转合同重复操作不重复创建台账记录。
5. 新旧数据库迁移、备份、恢复和覆盖升级通过。
6. 现有自动化回归不低于 `181 passed, 5 skipped`，新增采购测试全部通过。
7. 无 P0/P1 缺陷；所有跳过测试有环境原因和实机结果。
8. 性能达到第 12 节目标，或形成经批准的基线差异。
9. 用户手册、字段字典、标准报价模板说明和版本号与代码一致。
10. 构建可由固定命令从干净工作树重复完成。

PDF 输出继续调用现有能力，不在采购模块中重写转换引擎。Word/LibreOffice 的实机结果应单独记录，不能用 DOCX 自动化测试代替。

---

## 14. 风险与控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 非标准报价格式过多 | 导入范围失控 | 首版锁定标准模板；非标准格式先人工映射 |
| 物料名称无法可靠对齐 | 比价错误 | 标准模板携带稳定项目明细 ID；模糊匹配只做建议 |
| 税价口径和单位不一致 | 价格结论失真 | 导入时强制口径/单位校验，未统一前禁止比较 |
| 金额浮点误差 | 总价、阈值和合同金额不一致 | `Decimal` + 整数最小货币单位 |
| 自动推荐被误认为审批结论 | 业务责任风险 | 系统只提示，成交必须人工确认并留痕 |
| 项目修改覆盖历史结论 | 无法审计 | 比价、成交建议、数据单采用不可变版本快照 |
| 转合同重复或半成功 | 重复台账/悬空文件 | 幂等键、状态机、事务和补偿清理 |
| 新表破坏现有合同数据 | 升级失败 | 升级前备份、迁移回滚、v8 升级专项测试 |
| 文件绝对路径导致升级/迁移失效 | 文件不可下载 | 保存相对路径并集中解析 |
| 需求扩张为 ERP/SRM | 首版延期 | 按第 3 节范围和阶段退出标准控制 |
| 当前工作树不干净 | 无法定位回归来源 | M0 先建立独立分支/快照并记录已有改动 |

---

## 15. 开发前需确认的业务参数

以下问题不阻塞技术方案，但应在 M0 给出书面答案：

1. 项目编号规则。建议默认 `CG-YYYYMMDD-####`，允许人工修改但全局唯一。
2. 首版是否只处理 CNY；是否统一以含税价格比较。
3. 高低价默认阈值及不同采购类别是否允许不同配置。
4. 供应商名称的唯一判断规则，是否存在统一供应商编码。
5. 成交是否允许拆分给多家供应商；本方案数据模型支持明细级成交，页面首版可先限制单一供应商。
6. 未选择最低价时必须填写的原因和最小内容要求。
7. 首批需要的询价函、报价表、澄清函、成交建议和合同模板样例。
8. 采购项目、报价原件和归档包的保留年限。
9. 转合同后允许修改哪些上游数据，以及是否需要重新确认成交建议。
10. ERP/OA 摘要的字段清单；首版只输出文件，不直接写入外部系统。

在未获得额外规则前，建议采用本方案中的默认值：CNY、含税口径、正负 20% 高低价阈值、人工确认成交、非最低价必须说明、转合同先进入可复核草稿。

---

## 16. 首版实施记录（2026-06-23）

### 16.1 已实现

- 新增独立 `procurement_store`，在现有 SQLite 中建立采购项目、明细、供应商、项目文件、导入任务、报价、比价、澄清、谈判轮次、成交建议、合同数据单、合同关联和审计表。
- `RuntimePaths` 新增持久化采购项目目录；所有项目文件使用相对路径登记，并保存 SHA-256、版本和大小。
- 完成采购项目新增、编辑、状态流转、归档入口，以及采购明细和候选供应商的新增、编辑、删除。
- 完成询价函 Word 和供应商标准报价 Excel 模板生成。
- 完成标准 `.xlsx` 报价上传、项目/供应商/轮次/版本校验、金额重算、错误警告预览和确认入库。
- 支持同一供应商多轮报价，横向比价自动使用最新确认轮次。
- 完成横向比价、漏项、高低价、技术/商务偏离、交期、付款和税价口径提示，以及 Excel 导出。
- 完成澄清问题初稿、状态和回复管理，并可导出澄清问题 Word。
- 完成成交建议确认、完整报价约束、非最低有效价原因约束和成交建议 Word。
- 完成标准合同数据单、模板字段/明细映射、合同编辑器预填、合同生成以及采购项目到合同台账的关联回写。
- 打包资源改为递归包含 `templates/procurement/` 页面。
- README 已补充采购前置操作流程和首版范围。

### 16.2 自动化结果

执行：

```powershell
python -m pytest -q -p no:cacheprovider --tb=short --basetemp .pytest_basetemp_proc_release
```

结果：`188 passed, 5 skipped, 0 failed`。

新增测试覆盖采购表幂等初始化、既有合同数据保护、项目约束、合法/非法状态流转、报价模板、完整/漏项报价、多轮报价、异常比价、澄清、三类 Word 文件、成交约束、合同数据单、编辑器预填、合同台账关联、页面渲染和 CSRF 门禁。

### 16.3 首版验收时的后续项

以下为首版验收时记录的后续项；其中业务功能已在第 17 节补齐：

- 非标准 Excel 字段映射和 Word 报价表解析。
- 扫描 PDF/OCR、供应商在线填报、多用户权限和审批流。
- 完整谈判纪要、历史价格策略和本地模型辅助起草。
- Windows 安装包覆盖升级、浏览器实机 E2E 和大数据性能基线。

---

## 17. 业务功能补齐记录（2026-06-23）

在首版闭环基础上继续完成：

- 采购明细 Excel 复制粘贴、Excel 上传和导出。
- 非标准 Excel 字段映射导入。
- Word 报价表格选择、字段映射和导入。
- 文本型 PDF 表格解析；扫描 PDF 通过本机 Tesseract OCR 后进入同一映射流程。
- 项目级比价阈值、最小有效供应商数量和税价口径规则。
- 多轮报价变化、降价幅度、谈判轮次、供应商承诺、谈判纪要和最终承诺表。
- 按供应商生成澄清函。
- 整包成交和按明细拆分给多家供应商。
- 历史成交价格查询、中位价、建议目标价和谈判策略初稿。
- ERP/OA 填报摘要及包含清单、源文件和生成文件的完整归档包。

新增 Python 依赖已写入 `requirements.txt` 并安装。扫描件 OCR 代码已经接入，但本机仍需独立安装 Tesseract 中文语言包；可通过 `CT_TESSERACT_CMD` 配置路径。

最终自动化结果：

```text
192 passed, 5 skipped, 0 failed
```

仍然明确不属于本地业务工作台范围的事项：供应商在线门户、多用户权限、审批流、ERP 自动写入、电子签章和复杂法务审查。
